name: memscreen_flutter
description: MemScreen Flutter frontend — optional UI alongside Kivy. Connects to MemScreen HTTP API.
version: 0.6.3
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  path_provider: ^2.1.5
  package_info_plus: ^8.0.2
  provider: ^6.1.0
  video_player: ^2.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
