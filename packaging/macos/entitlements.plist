<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow loading of code from arbitrary locations (needed for PyInstaller) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow loading of unsigned code (needed for Python interpreter) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow execution of code from arbitrary locations -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allow execution of external code (needed for subprocess calls) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Network access (needed for Ollama communication) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <key>com.apple.security.network.server</key>
    <true/>

    <!-- File access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Audio recording -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Camera access (for future video features) -->
    <key>com.apple.security.device.camera</key>
    <true/>
</dict>
</plist>
