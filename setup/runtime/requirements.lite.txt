# Lightweight runtime dependencies for packaged MemScreen app (no bundled local models).
# Goal: recording + process tracking + video timeline + optional chat connectivity.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
requests>=2.32.0
httpx>=0.27.0
ollama>=0.5.0
PyYAML>=6.0.2
psutil>=6.0.0


# macOS multi-screen + window APIs

# Optional but recommended: audio/video merge path for recordings
moviepy>=1.0.3
imageio-ffmpeg>=0.6.0
